오라클 제약조건, 인덱스생성, 검색 AUTOTRACE ON 활용


SELECT * FROM USER_CONSTRAINTS
WHERE TABLE_NAME = 'EMP_BOSS';


SELECT * FROM USER_CONS_COLUMNS
WHERE TABLE_NAME ='EMP_BOSS';

CREATE INDEX INX_ENM1 ON EMP_BOSS(EMP_NAME);

SELECT * FROM USER_IND_COLUMNS
WHERE TABLE_NAME = 'EMP_BOSS';



SET AUTOTRACE ON
SELECT /*+ INDEX(EMP_BOSS, INX_ENM1) */ EMP_NAME,
BOSS_NAME
FROM EMP_BOSS;

SET AUTOTRACE ON
SELECT LPAD(EMP_NAME, LENGTH(EMP_NAME) + (LEVEL*5)-2,'_') AS LEVELDEEP,EMP_ID, BOSS_ID
FROM EMP_BOSS
CONNECT BY PRIOR EMP_ID = BOSS_ID;